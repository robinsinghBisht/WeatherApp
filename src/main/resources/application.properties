# Railway provides PORT at runtime; 9090 remains the local/Docker default.
server.port=${PORT:9090}
# Set OPENWEATHERMAP_API_KEY (or the legacy `key`) before starting Spring Boot.
openweathermap.api.key=${OPENWEATHERMAP_API_KEY:${key:}}
